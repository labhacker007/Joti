# ============================================================================
# Joti Platform - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in the required values:
#   cp .env.example .env
#
# Required values are marked with CHANGEME — the app will refuse to start
# until you replace them.
#
# After starting the app, create your first admin user with:
#   docker-compose exec backend python manage.py createsuperuser
# ============================================================================

# --- Database ---------------------------------------------------------------
POSTGRES_USER=joti
POSTGRES_PASSWORD=CHANGEME
POSTGRES_DB=joti

# --- Security ---------------------------------------------------------------
# SECRET_KEY must be 32+ chars and NOT contain "dev-secret-key" or "change-me"
SECRET_KEY=CHANGEME

# --- Service URLs (auto-wired for Docker, override for external DBs) -------
# DATABASE_URL is built automatically from POSTGRES_* vars in docker-compose.
# Only set this if you are pointing at an external database:
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname
REDIS_URL=redis://redis:6379/0

# --- Frontend ---------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:8000/api
CORS_ORIGINS=http://localhost:3000

# --- Ports (change if 3000/8000 are taken) ----------------------------------
FRONTEND_PORT=3000
BACKEND_PORT=8000

# --- Scheduler --------------------------------------------------------------
ENABLE_AUTOMATION_SCHEDULER=true

# --- Debug (set to true for development) ------------------------------------
DEBUG=true
ENV=dev
